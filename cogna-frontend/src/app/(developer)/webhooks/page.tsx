'use client';
import { FormEvent, useCallback, useEffect, useState } from 'react';
import { RefreshCw, Send, Webhook } from 'lucide-react';
import { api } from '@/lib/api';
import { getErrorMessage } from '@/lib/error-message';
import CustomerPortalNav from '@/components/layout/customer-portal-nav';
type Delivery={id:string;eventId:string;eventType:string;statusCode:number|null;response:string|null;success:boolean;createdAt:string};
export default function WebhooksPage(){const [url,setUrl]=useState('');const [secret,setSecret]=useState('');const [deliveries,setDeliveries]=useState<Delivery[]>([]);const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [error,setError]=useState<string|null>(null);const load=useCallback(async()=>{setLoading(true);try{const [config,logs]=await Promise.all([api.get('/developer/webhooks'),api.get('/developer/webhooks/deliveries')]);if(config.data.success&&config.data.data){setUrl(config.data.data.url);setSecret(config.data.data.secret)}if(logs.data.success)setDeliveries(logs.data.data)}catch(e:unknown){setError(getErrorMessage(e,'Unable to load webhook configuration.'))}finally{setLoading(false)}},[]);useEffect(()=>{const t=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(t)},[load]);async function save(e:FormEvent){e.preventDefault();setSaving(true);setError(null);try{const r=await api.post('/developer/webhooks',{url,secret});if(!r.data.success)throw new Error(r.data.message)}catch(e:unknown){setError(getErrorMessage(e,'Unable to save webhook endpoint.'))}finally{setSaving(false)}}async function replay(id:string){try{const r=await api.post(`/developer/webhooks/deliveries/${id}/retry`);if(!r.data.success)throw new Error(r.data.message);await load()}catch(e:unknown){setError(getErrorMessage(e,'Unable to replay delivery.'))}}return <main className="min-h-screen bg-[#062C23] text-white"><CustomerPortalNav/><div className="mx-auto max-w-6xl px-5 py-10 sm:px-8"><p className="text-xs font-bold uppercase tracking-[.22em] text-[#F8D56B]">Developer webhooks</p><h1 className="mt-3 font-display text-4xl font-bold">Stay in sync with Cogna events.</h1><p className="mt-2 text-sm text-emerald-100/65">Configure one endpoint and inspect the real delivery history. Your signing secret is never displayed in delivery logs.</p>{error&&<p className="mt-6 text-sm text-rose-100">{error}</p>}<div className="mt-8 grid gap-6 lg:grid-cols-[.9fr_1.4fr]"><form onSubmit={save} className="rounded-[2rem] border border-emerald-100/15 bg-white/[0.07] p-6"><div className="flex items-center gap-2"><Webhook className="text-[#F8D56B]" size={20}/><h2 className="font-display text-xl font-bold">Endpoint setup</h2></div><label className="mt-6 block text-xs font-bold text-emerald-100/70">Destination URL<input required type="url" value={url} onChange={e=>setUrl(e.target.value)} className="mt-2 w-full rounded-2xl border border-emerald-100/15 bg-[#062C23]/70 px-4 py-3 text-sm" placeholder="https://example.com/webhooks/cogna"/></label><label className="mt-4 block text-xs font-bold text-emerald-100/70">Signing secret<input required type="password" value={secret} onChange={e=>setSecret(e.target.value)} className="mt-2 w-full rounded-2xl border border-emerald-100/15 bg-[#062C23]/70 px-4 py-3 text-sm"/></label><button disabled={saving} className="mt-6 rounded-full bg-[#D4AF37] px-5 py-3 text-sm font-bold text-[#062C23] disabled:opacity-50">{saving?'Saving…':'Save endpoint'}</button></form><section className="rounded-[2rem] border border-emerald-100/15 bg-white/[0.05] p-6"><div className="flex items-center justify-between"><h2 className="font-display text-xl font-bold">Delivery history</h2><button onClick={()=>void load()} className="text-[#F8D56B]"><RefreshCw className={loading?'animate-spin':''} size={17}/></button></div><div className="mt-5 divide-y divide-emerald-100/10">{deliveries.length?deliveries.map(d=><div key={d.id} className="flex flex-wrap items-center justify-between gap-4 py-4"><div><p className="font-bold">{d.eventType}</p><p className="mt-1 font-mono text-xs text-emerald-100/45">{d.eventId} · {new Date(d.createdAt).toLocaleString()}</p></div><div className="flex items-center gap-3"><span className={d.success?'text-sm font-bold text-emerald-200':'text-sm font-bold text-rose-200'}>{d.statusCode||'FAILED'}</span><button onClick={()=>void replay(d.id)} className="rounded-full border border-emerald-100/15 p-2 text-[#F8D56B]"><Send size={14}/></button></div></div>):<p className="py-8 text-sm text-emerald-100/60">No deliveries have been recorded yet.</p>}</div></section></div></div></main>}