'use client';
import { FormEvent, useCallback, useEffect, useState } from 'react';
import { Copy, KeyRound, Loader2, Trash2 } from 'lucide-react';
import { api } from '@/lib/api';
import { getErrorMessage } from '@/lib/error-message';
import CustomerPortalNav from '@/components/layout/customer-portal-nav';
type KeyItem = { id:string; name:string; apiKey:string; environment:'TEST'|'LIVE'; scopes:string[]; expiresAt:string|null; status:'ACTIVE'|'REVOKED' };
const scopes = ['read:catalog','write:orders','read:orders'];
export default function KeysPage() {
 const [keys,setKeys]=useState<KeyItem[]>([]); const [name,setName]=useState(''); const [environment,setEnvironment]=useState<'TEST'|'LIVE'>('TEST'); const [selected,setSelected]=useState<string[]>(['read:catalog']); const [raw,setRaw]=useState<string|null>(null); const [loading,setLoading]=useState(true); const [creating,setCreating]=useState(false); const [error,setError]=useState<string|null>(null);
 const load=useCallback(async()=>{setLoading(true);setError(null);try{const r=await api.get('/developer/keys');if(!r.data.success)throw new Error(r.data.message);setKeys(r.data.data)}catch(e:unknown){setError(getErrorMessage(e,'Unable to load API keys.'))}finally{setLoading(false)}},[]);
 useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer)},[load]);
 async function create(e:FormEvent){e.preventDefault();setCreating(true);try{const r=await api.post('/developer/keys',{name,environment,scopes:selected});if(!r.data.success)throw new Error(r.data.message);setRaw(r.data.data.rawKey);setName('');await load()}catch(e:unknown){setError(getErrorMessage(e,'Unable to create API key.'))}finally{setCreating(false)}}
 async function revoke(id:string){if(!confirm('Revoke this key? Integrations using it will stop working.'))return;try{const r=await api.delete(`/developer/keys/${id}`);if(!r.data.success)throw new Error(r.data.message);await load()}catch(e:unknown){setError(getErrorMessage(e,'Unable to revoke API key.'))}}
 return <main className="min-h-screen bg-[#062C23] text-white"><CustomerPortalNav/><div className="mx-auto max-w-6xl px-5 py-10 sm:px-8"><p className="text-xs font-bold uppercase tracking-[.22em] text-[#F8D56B]">Developer keys</p><h1 className="mt-3 font-display text-4xl font-bold">Scoped access, under your control.</h1>{error ? <p className="mt-6 rounded-2xl border border-rose-300/25 bg-rose-950/30 p-4 text-sm text-rose-100">{error}</p> : null}<form onSubmit={create} className="mt-8 rounded-[2rem] border border-emerald-100/15 bg-white/[0.07] p-6 shadow-premium-dark"><h2 className="flex items-center gap-2 font-display text-xl font-bold"><KeyRound className="text-[#F8D56B]"/>Create an API key</h2><div className="mt-6 grid gap-4 md:grid-cols-2"><label className="text-xs font-bold text-emerald-100/70">Key name<input required value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-2xl border border-emerald-100/15 bg-[#062C23]/70 px-4 py-3 text-sm"/></label><label className="text-xs font-bold text-emerald-100/70">Environment<select value={environment} onChange={e=>setEnvironment(e.target.value as 'TEST'|'LIVE')} className="mt-2 w-full rounded-2xl border border-emerald-100/15 bg-[#062C23]/70 px-4 py-3 text-sm"><option value="TEST">TEST</option><option value="LIVE">LIVE</option></select></label></div><div className="mt-5 flex flex-wrap gap-3">{scopes.map(scope=><label key={scope} className="rounded-full border border-emerald-100/15 px-4 py-2 text-xs font-bold text-emerald-100/75"><input className="mr-2 accent-[#D4AF37]" type="checkbox" checked={selected.includes(scope)} onChange={()=>setSelected(v=>v.includes(scope)?v.filter(x=>x!==scope):[...v,scope])}/>{scope}</label>)}</div><button disabled={creating||selected.length===0} className="mt-6 rounded-full bg-[#D4AF37] px-5 py-3 text-sm font-bold text-[#062C23]">{creating?'Creating…':'Create API key'}</button>{raw ? <div className="mt-6 rounded-2xl border border-amber-200/30 bg-amber-950/25 p-4"><p className="font-bold text-amber-100">Copy this key now. It will not be shown again.</p><div className="mt-3 flex gap-2 rounded-xl bg-black/25 p-3 font-mono text-xs"><span className="flex-1 break-all">{raw}</span><button type="button" onClick={()=>void navigator.clipboard.writeText(raw)}><Copy size={16}/></button></div></div> : null}</form><section className="mt-8 rounded-[2rem] border border-emerald-100/15 bg-white/[0.05] p-6"><h2 className="font-display text-xl font-bold">Your keys</h2>{loading ? <Loader2 className="mt-8 animate-spin text-[#F8D56B]"/> : <div className="mt-5 divide-y divide-emerald-100/10">{keys.length===0 ? <p className="py-8 text-sm text-emerald-100/60">No keys have been created yet.</p> : keys.map(key=><div key={key.id} className="flex flex-wrap items-center justify-between gap-4 py-4"><div><p className="font-bold">{key.name}</p><p className="mt-1 font-mono text-xs text-emerald-100/50">{key.apiKey}</p><p className="mt-2 text-xs text-emerald-100/65">{key.environment} · {key.scopes.join(', ')} · {key.expiresAt ? new Date(key.expiresAt).toLocaleDateString() : 'No expiry'}</p></div>{key.status==='ACTIVE' ? <button type="button" onClick={()=>void revoke(key.id)} className="rounded-full border border-rose-200/30 p-2 text-rose-100"><Trash2 size={14}/></button> : <span className="text-xs font-bold text-emerald-100/45">REVOKED</span>}</div>)}</div>}</section></div></main>
}